// Could not get this to work 
// https://github.com/kyleconroy/sqlc/issues/654
version: "1"
packages:
  - name: "postgres"
    path: "postgres"
    emit_json_tags: true
    schema: "schema.sql"
    queries: "queries.sql"